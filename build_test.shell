#!/bin/bash
# 进入到你的 Maven 项目目录，假设项目目录为 your-project-directory，需替换为实际目录名
# 获取当前脚本所在的目录
script_dir=$(dirname "$(realpath "$0")")
echo "当前脚本所在目录: $script_dir"
# shellcheck disable=SC2164
cd "$script_dir"
# 执行 mvn package 命令
mvn package